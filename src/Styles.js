import { StyleSheet } from "react-native"

export default StyleSheet.create({
    appView: {
        flex: 1,
    },
    navigator: {
        tabBarStyle: {
            height: 0
        },
        swipeEnabled: false,
    }
});
